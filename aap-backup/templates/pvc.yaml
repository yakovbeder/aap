apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aap-backup-pvc
  namespace: {{ .Values.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.storageSize }}
  storageClassName: backup-retain
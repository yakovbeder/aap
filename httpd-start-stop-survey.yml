---
- name: Demo – Manage HTTPD package via Survey
  hosts: all
  gather_facts: false

  tasks:
    - name: Ensure Apache HTTP server is in the desired state
      ansible.builtin.service:
        name: httpd
        state: "{{ httpd_service_state }}"
